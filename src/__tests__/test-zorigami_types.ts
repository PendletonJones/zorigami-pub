import {
    isMessagePort,
    isCustomPort,
    isPromisedPostMessage
} from '../zorigami_types';

describe('suite', () => {
    test('should fail', () => {
        expect(1 + 1).toEqual(1);
    });
});